import React from "react";
import { Text, View, SafeAreaView } from "react-native";

const AddPlayerScreen = () => {
  return (
    <SafeAreaView>
      <View style={{ justifyContent: "center", alignItems: "center" }}>
        <Text>Add Player Screen</Text>
      </View>
    </SafeAreaView>
  );
};

export default AddPlayerScreen;

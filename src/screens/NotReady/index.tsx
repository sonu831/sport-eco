import React from "react";
import { Text, View } from "react-native";

const NotReady = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 16 }}>Work In-Progress for this screen</Text>
    </View>
  );
};

export default NotReady;
